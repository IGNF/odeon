from typing import List


def loop_in_weighted_space(weights: List,
                            interval: int = 1,
                            starting_point: int = 0,
                            oversampling: bool = False,
                            max_cycle_number: int = 10):

    _has_started = False


def pick_random_starting_point():
    ...
