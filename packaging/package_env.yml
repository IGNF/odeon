name: odeon-env
channels:
  - pytorch
  - conda-forge
  - defaults
dependencies:
  - pytorch>=1.11.0
  - cudatoolkit=10.2
  - pip>=22.0.4
  - python>=3.10
  - geopandas>=0.10
  - rasterio
  - pip:
      - -r requirements.txt
